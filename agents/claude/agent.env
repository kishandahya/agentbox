PRESET_API_VERSION=1
AGENT_ID=claude
BASE_IMAGE=ubuntu:24.04
DEFAULT_CMD=/bin/bash
DEFAULT_WORKDIR=/workspace
# Low-priority preset. ChatGPT OAuth is the default auth for other presets.
# If actually used with Anthropic's API, set ANTHROPIC_API_KEY in .env.
REQUIRED_ENV_VARS=""
START_MODE=manual
